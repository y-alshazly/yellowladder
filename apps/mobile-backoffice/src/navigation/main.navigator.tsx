import { MainDrawerNavigator } from './main-drawer.navigator';

/**
 * Main navigator. A single slide-over drawer is used on phones and tablets —
 * always closed by default, opened via the header's menu button.
 */
export function MainNavigator() {
  return <MainDrawerNavigator />;
}
